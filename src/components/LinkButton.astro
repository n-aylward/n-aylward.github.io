---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"a">;

const { class: className, ...attrs } = Astro.props;
---

<a class:list={["group inline-flex items-center gap-1 hover:text-accent", className]} {...attrs}>
  <slot />
</a>
